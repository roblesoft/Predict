<%= form_with(model: cicle, local: true) do |form| %>
  <% if cicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cicle.errors.count, "error") %> prohibited this cicle from being saved:</h2>

      <ul>
      <% cicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.number_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
