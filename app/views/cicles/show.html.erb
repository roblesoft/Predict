<p id="notice"><%= notice %></p>

<div class="card">
<div class="card-body">
<div class="row">
<div class="col">

<h1 class="display-1">Meses</h1>


<table class="table">
  <thead>
    <tr>
      <th>Mes</th>
      <th>Año</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stages.each do |month| %>
      <tr>
        <td><%= month.name %></td>
        <td><%= link_to 'Mostrar', cicle_stage_path(@cicle, month), class: "btn btn-primary" %></td>
        <td><%= link_to 'Eliminar', cicle_stage_path(@cicle, month), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
    <h5 class="display-5">Nuevo mes</h5>
  <%= render 'stages/form' %>
</div>
</div>
</div>

</div>


<%= link_to 'Edit', edit_cicle_path(@cicle) %> |
<%= link_to 'Back', cicles_path %>
